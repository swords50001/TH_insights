version: 1.0
runtime: nodejs
build:
  commands:
    build:
      - npm ci
      - npm run build
env:
  - name: VITE_API_URL
    value: http://localhost:8080
run:
  command: nginx -g 'daemon off;'
  port: 80
