# Backend environment example for TH_insights
# Copy this to .env in the backend folder and fill values.

JWT_SECRET=zm+KbE2/v1hqWD40Na5VBtnIyrDIWqwYK07PYrIGcV8=
DB_HOST=th-db-insights.cx4mmeokynqf.us-west-2.rds.amazonaws.com
DB_USER=thadmin
DB_PASSWORD=DJJAiKQVI0RTVHK
DB_NAME=th-db-insights
# Optional: override port (server currently uses 8080 by default)
PORT=8080
# Maximum number of rows returned for card queries (default 1000)
MAX_ROWS=1000

# Optional SQL allowlist. To enable, set ALLOWLIST_ENABLED=true and list comma-separated card ids.
# Example: ALLOWLIST_ENABLED=true
#          ALLOWED_CARD_IDS=1,2,5
ALLOWLIST_ENABLED=false
ALLOWED_CARD_IDS=
